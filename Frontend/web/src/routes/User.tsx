import AvailableRooms from "../Components/AvailableRooms.tsx";

const User = () => {
    return (
        <div >
            <AvailableRooms isAdmin={false} />
        </div>
    );
};

export default User;
